const add = (x: number, y: number) => x + y;

function addFn(x: number, y: number) {
  return x + y;
}

const addSeveral = (a: number, b: number, c: number, d: number, e: number) => (
  a + b +
  c +
  d +
  e
);

export {}